[package]
name = "agent-os-runtime"
version = "0.1.0"
description = "Runtime orchestration engine"
requires-python = ">=3.12"
dependencies = [
    "msgspec>=0.18.0",
    "redis>=5.0.0",
    "sqlalchemy>=2.0.0",
    "asyncpg>=0.29.0",
    "httpx>=0.27.0",
]

[tool.uv]
sources = { workspace = true }