[package]
name = "agent-os-agents"
version = "0.1.0"
description = "Agent implementations"
requires-python = ">=3.12"
dependencies = [
    "msgspec>=0.18.0",
    "httpx>=0.27.0",
]

[tool.uv]
sources = { workspace = true }